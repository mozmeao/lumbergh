netlify:
  type: deploy
  script:
    # Stage
    - curl -X POST -d {} ${WEBHOOK_STAGE}
    # Prod
    - curl -X POST -d {} ${WEBHOOK_PROD}
    # Dead Man's Snitch Ping
    - curl ${DMS}
  only:
    - schedules
